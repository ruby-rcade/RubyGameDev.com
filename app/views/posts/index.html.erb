<h1>
  <% if params[:search].present? %>
    Post Search: "<%= params[:search] %>"
  <% else %>
    <%= t('post.latest_post') %>
  <% end %>
</h1>

<% if @posts.size == 0 %>
  <div class="alert alert-info">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    <span><%= t('post.result') %></span>
  </div>
<% end %>

<%= render 'list' %>
